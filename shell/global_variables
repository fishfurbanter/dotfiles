export IRC_CLIENT="weechat"
export IRC_NICKNAME=""
export GOPATH=${HOME}/.go
export WALLPAPER_DIR="${HOME}/.wallpapers"
export UTILS=${HOME}/.utils
export PLATFORM="$(get_platform)"

####
# Python env vars
####
export PYTHONDONTWRITEBYTECODE=1

####
# Powerline
####
## Not using powerline atm
# export POWERLINE_REPO=$(pip3 show powerline-status | grep 'Location:' |awk '{print $2}')
# export POWERLINE_TMUX_CONF=${POWERLINE_REPO}/powerline/bindings/tmux/powerline.conf

####
# Dotfiles
####

export DOTFILES_DIR="${HOME}/.dotfiles"

####
# Go
####
export GOPATH=${HOME}/.go

####
# VPN
####
export OPENVPN_PROFILE_DIR=~/.openvpn

####
# Monitors
# Colon delimited, should be arranged in order from left to right
####

export MONITORS=DP-2:HDMI-1:DP-1
export MONITOR_LEFT=$(echo $MONITORS | cut -d ':' -f 1)
export MONITOR_MAIN=$(echo $MONITORS | cut -d ':' -f 2)
export MONITOR_RIGHT=$(echo $MONITORS | cut -d ':' -f 3)

####
# Torrents
####

export TRANSMISSION_HOST=0.0.0.0
export TRANSMISSION_PORT=9091
